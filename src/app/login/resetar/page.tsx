import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Resetar a senha | Dogs',
  description: 'Resetar a sua senha do Dogs.',
};

export default async function ResetarPage() {
  return (
    <main>
      <h1>Resetar</h1>
    </main>
  );
}
